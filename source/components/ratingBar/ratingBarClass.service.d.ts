export interface IRatingBarClassService {
    getClass(confidence: number): string;
}
export declare class RatingBarClassService implements IRatingBarClassService {
    getClass(confidence: number): string;
}
